import { colors } from '../theme/dark'

export const GRADIENT = `linear-gradient(to bottom right, orange, transparent, orange)`
export const GRADIENT_2 = `linear-gradient(45deg, transparent, transparent, cyan)`
export const GRADIENT_GRAY = `linear-gradient(to bottom right, ${colors.sec[100]}, transparent, ${colors.sec[200]})`
